#version 400 core

in vec2 pass_textureCoords;

uniform sampler2D textureSampler;

out vec4 out_Colour;

void main(void) {
	float textAlpha = texture(fontAtlas, pass_texCoords).a;
    out_Color = vec4(color, alpha * textAlpha);
    
}
